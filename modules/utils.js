const esc=(v)=>{if(typeof v!='string')return String(v??'');return v.replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))};const rub=new Intl.NumberFormat('ru-RU',{style:'currency',currency:'RUB',minimumFractionDigits:0,maximumFractionDigits:0});const formatMoney=(n)=>rub.format(Number(n)||0);const rawNorm=(v)=>String(v??'').toLowerCase().replace(/ё/g,'е').replace(/,/g,'.').replace(/["'«»()\[\];:/\\|+_]+/g,' ').replace(/-/g,' ').replace(/\s+/g,' ').trim();const compactKey=(v)=>String(v??'').toLowerCase().replace(/ё/g,'е').replace(/[^0-9a-zа-я]+/gi,'');const searchTokens=(q)=>{const src=rawNorm(q);return(src.match(/\d+[a-zа-я]+|[a-zа-я]+\d+|\d+(?:\.\d+)?|[a-zа-я]+/gi)||[]).map(x=>x.toLowerCase()).filter(Boolean).filter(x=>x!='u'&&x!='ю')};const unique=(list,key)=>[...new Set(list.map(p=>p[key]).filter(v=>v!=''&&v!=null))].sort((a,b)=>String(a).localeCompare(String(b),'ru',{numeric:true}));const hasValue=(v)=>{if(v===undefined||v===null)return false;const s=String(v).trim();return s!=''&&s!='0'&&s!='0.00'};const getUnit=(p)=>(p&&p.unit)?p.unit:'шт.';const getMaxAvailable=(p)=>Math.max(Number(p?.qty||0),Number(p?.transit||0));const getStockLabel=(p)=>{const u=getUnit(p);if(Number(p?.qty||0)>0)return`В наличии: ${p.qty} ${u}`;if(Number(p?.transit||0)>0)return`Транзит: ${p.transit} ${u}`;return'Под заказ'};const getStockChip=(p)=>{const u=getUnit(p);if(Number(p?.qty||0)>0)return`<span class="stock-chip ok">В наличии: ${esc(p.qty)} ${esc(u)}</span>`;if(Number(p?.transit||0)>0)return`<span class="stock-chip warn">Транзит: ${esc(p.transit)} ${esc(u)}</span>`;return`<span class="stock-chip order">Под заказ</span>`};const extractLength=(p)=>{const source=[p.article,p.name].filter(Boolean).join(' ');const m=source.match(/(?:^|[-\s])(\d+(?:[.,]\d+)?)\s*m(?:[-\s]|$)/i)||source.match(/(\d+(?:[.,]\d+)?)\s*м\b/i);return m?m[1].replace(',','.')+'м':''};const storage={set:(key,value)=>{try{localStorage.setItem(key,JSON.stringify(value))}catch(e){console.warn(`Storage: ${e.message}`)}},get:(key,def=null)=>{try{const item=localStorage.getItem(key);return item?JSON.parse(item):def}catch(e){console.warn(`Storage: ${e.message}`);return def}},remove:(key)=>{try{localStorage.removeItem(key)}catch(e){console.warn(`Storage: ${e.message}`)}}};